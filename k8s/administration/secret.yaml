apiVersion: v1
kind: Secret
metadata:
  name: administration-secrets
  namespace: mymeetings
  labels:
    app: administration-service
type: Opaque
# Values below are base64 encoded placeholders. Replace with actual values before applying.
# Generate base64 values with: echo -n 'your-value' | base64
data:
  db-connection-string: REPLACE_WITH_BASE64_ENCODED_CONNECTION_STRING
  aws-access-key-id: REPLACE_WITH_BASE64_ENCODED_ACCESS_KEY
  aws-secret-access-key: REPLACE_WITH_BASE64_ENCODED_SECRET_KEY
